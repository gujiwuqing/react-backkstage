// import { useModel } from 'umi';
// export default function getTheme() {
//     const { themeColor } = useModel('user', (model) => ({
//         themeColor: model.themeColor,
//     }));
//     return {
//         'primary-color': themeColor,
//         '@backgroundColor': '#fff',
//         '@color': '#fff',
//     }
// }
// const { themeColor } = useModel('user', (model) => ({
//   themeColor: model.themeColor,
// }));
module.exports = {
  'primary-color': '#1DA57A', // Ant Design 的主色调
};
